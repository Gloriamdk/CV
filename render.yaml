services:
  - type: web
    name: mycv-web
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_AI_API_KEY
        fromSecret: <your-google-ai-secret>
